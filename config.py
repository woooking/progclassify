
# debug = True
debug = False

rewrite = True
# rewrite = False

wordembedding_rnn_config = {
    "window_size": 2,
    "batch_size": 50,
    "embedding_size": 128,
    "number_sampled": 64,
    "word_embedding_steps": 100000,
    "rnn_hidden_size": 100
}